@*@model IEnumerable<CapstoneProject.Project>

@{
    ViewBag.Title = "Project List";
}

<h2>Project List</h2>

<p>
    @Html.ActionLink("Create Project", "Create", null, new { @class = "btn btn-info" })
</p>
@{ if (Model != null && Model.Count() > 0)
    {
        @Html.Partial("~/Views/Projects/_Projects.cshtml", Model.ToList())
    }
    else
    {
        <p>You currently don't have any projects.</p>
    }
}*@
